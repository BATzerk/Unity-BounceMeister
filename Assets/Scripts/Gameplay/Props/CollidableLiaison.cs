using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Required for every Collidable Prop.
/// Put me on the GO that's got the Collider2D.
/// </summary>
//public class CollidableLiaison : MonoBehaviour {
    //// References
    //public Collidable MyCollidable { get; private set; }
    
    
    //// ----------------------------------------------------------------
    ////  Awake
    //// ----------------------------------------------------------------
    //private void Awake() {
    //    // Find MyCollidable!
    //    MyCollidable = GetComponent<Collidable>(); // try on my own GO.
    //    if (MyCollidable == null) { // No Collidable on my GO? Try my parent, then!
    //        MyCollidable = GetComponentInParent<Collidable>();
    //    }
    //    if (MyCollidable == null) {
    //        Debug.LogError("Whoa, ColliableLiason doesn't have a parent with a Collidable!");
    //    }
    //}
    

    //// ----------------------------------------------------------------
    ////  Events
    //// ----------------------------------------------------------------
    //private void OnTriggerEnter2D(Collider2D col) {
    //    if (MyCollidable != null) {
    //        MyCollidable.OnTriggerEnter2D(col);
    //    }
    //}
    //private void OnTriggerExit2D(Collider2D col) {
    //    if (MyCollidable != null) {
    //        MyCollidable.OnTriggerExit2D(col);
    //    }
    //}
    
    
//}
